# Server Configuration
PORT=5000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/saas-multi-tenant

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# SAAS Owner Credentials (for initial setup)
SAAS_OWNER_EMAIL=admin@saasplatform.com
SAAS_OWNER_PASSWORD=changethispassword

# CORS - Comma-separated list of allowed origins
# For production, add your Vercel frontend URL: https://your-frontend.vercel.app
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Frontend URL (for redirects after OAuth)
FRONTEND_URL=http://localhost:3001

# Allowed Frontend URLs for OAuth (comma-separated, for multi-domain support)
# Add your custom domain AND Vercel URL here
ALLOWED_FRONTEND_URLS=http://localhost:3001,https://your-app.vercel.app,https://your-custom-domain.com

# Email Configuration (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-email-password
